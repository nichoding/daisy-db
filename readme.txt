daisy-db is a simple java or-mapping framework that supports only basic data types.
does not support the Boolean type,does not support foreign key associations.
Use the commons-lang3 toolkit,c3p0 as connection pool..